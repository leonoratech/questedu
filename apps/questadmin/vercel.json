{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps --no-workspaces",
  "buildCommand": "NODE_TLS_REJECT_UNAUTHORIZED=0 npm run build",
  "env": {
    "NODE_TLS_REJECT_UNAUTHORIZED": "0"
  }
}
